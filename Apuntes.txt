Estandar recomendado para la importacion de modulos:
    const fs = requiere('node:fs')


Instalar express:
    npm install express --save

Instalar ESLINT:
    npm install eslint --save-dev
    or
    npm init @eslint/config

Instalacion de dependencias:
    Dev:
    - 
    producction:

Generacion de modelos con
    - npm install -g sequelize-cli (Recomiendo instalacion global)
    - npm install mysql2 (agregar al excel)
    - sequelize init
    Generar modelo:
    - npx sequelize-cli model:generate --name movie --attributes firstName:string,lastName:string,email:string
    Migrar modelos:
    - sequelize db:migrate

Bibliografia:

    https://github.com/CarlosCalderon01/MyAppLB

    https://expressjs.com/es/

    https://eslint.org/docs/latest/use/getting-started

// ----- // ----- // ----- // ----- // ----- // ----- //

HTTP

Request --> Peticion
Response --> Respuesta

// ----- // ----- // ----- // ----- // ----- // ----- //